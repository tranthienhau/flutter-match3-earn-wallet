name: flutter_match3_earn_wallet
description: "Level-to-Earn Memory Match game: geometric-shape match-3, energy + rewarded hints, virtual wallet with 2/3-1/3 revenue split, Net-30 payout queue, OPay disbursement, Play Integrity anti-fraud - Flutter + Riverpod POC."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
